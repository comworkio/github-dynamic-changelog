stages:
  - publish
  - deliver

mirror:
  stage: publish
  script:
    - setsid ./ci/mirror.sh
  only:
    - /^(main.*)$/
  tags:
    - github-changelog

build_and_deliver:
  stage: deliver
  script:
    - setsid ./ci/docker-deliver.sh "x86" "github-dynamic-changelog" "1.2"
  only:
    refs:
      - /^(main.*)$/
    changes:
      - .gitlab-ci.yml
      - api.py
      - requirements.txt
      - ci/docker-deliver.sh
      - docker-compose-build-x86.yml
      - Dockerfile
  tags:
    - github-changelog
